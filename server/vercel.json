{
  "version": 2,
  "builds": [
    { "src": "src/app.ts", "use": "@vercel/node" },
    {"src": "src/**/*", "use": "@vercel/static" }
  ],
  "routes": [
    { "src": "/(.*)", "dest": "src/app.ts" }
  ],
  "installCommand": "npm install --legacy-peer-deps"
}

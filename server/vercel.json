{
  "version": 2,
  "builds": [
    { "src": "src/api/app.ts", "use": "@vercel/node" },
    {"src": "src/**/*", "use": "@vercel/static" }
  ],
  "routes": [
    { "src": "/(.*)", "dest": "src/api/app.ts" }
  ],
  "installCommand": "npm install --legacy-peer-deps"
}
